## 背景

接口文档页在接口数量较多时会等待 Stoplight Elements 组件脚本和 `/api.json` 文档解析完成。在这段等待期间页面没有明确的加载状态，用户会看到空白页面并误判为加载失败。同时，中文语言环境下仍有部分接口标题显示英文，说明 `zh-CN` 的接口文档本地化资源覆盖存在缺口。

本变更聚焦修复接口文档页面的加载反馈和中文接口标题本地化完整性，不改变宿主核心 API 契约、业务数据模型或插件运行时边界。
