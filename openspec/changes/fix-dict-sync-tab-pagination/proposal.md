## 背景

GitHub issue #40反馈两个默认管理工作台行为问题：

1. 修改字典数据的标签文字后，已经打开的业务列表中对应字典标签没有同步更新。
2. 切换工作台 Tab后，前一个 Tab页面中的表格分页状态丢失。

已确认根因分别是前端字典 Store清空`Map`后没有更新已挂载组件持有的旧数组引用，以及后端菜单 seed中列表页面`is_cache=0`导致`/menus/all`返回`meta.keepAlive=false`。本变更修复默认管理工作台的前端缓存刷新语义和内建菜单缓存配置，不引入兼容分支。
